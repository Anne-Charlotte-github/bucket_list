<nav class="h-12 w-full fixed bottom-0 inset-x-0 z-50 flex items-center justify-between flex-wrap bg-blue-900 text-white mt-0 ">
  <div class="w-full block flex-grow mx-2 md:mx-10 flex justify-center">
    <%= link_to root_path, class:class_for_link_to(root_path) do %>
      <%= responsive_html_for_navbar('🌟','My bucket list') %>
    <% end %>
    <%= link_to root_path, class:class_for_link_to(root_path) do %>
      <%= responsive_html_for_navbar('👨‍👩‍👧‍👧',"My friends' lists") %>
    <% end %>
    <%= link_to root_path, class:class_for_link_to(root_path)do %>
      <%= responsive_html_for_navbar('🗓','Dreams schedule') %>
    <% end %>
    <div class="absolute right-2 md:right-10 flex items-center">
      <% if current_user %>
        <div class="pr-2 hidden md:inline-block"> Logged as <%= "#{current_user.nickname}"%></div>
      <% end %>
      <%= link_to root_path, class:class_for_link_to(root_path) do %>
        <%= responsive_html_for_navbar('⚙️','Settings') %>
      <% end %>
    </div>
  </div>
</nav>
